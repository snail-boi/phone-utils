<UserControl x:Class="phone_utils.Intent_Sender"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:phone_utils"
             mc:Ignorable="d"
             Background="#661E1E1E">

    <UserControl.Resources>
        <!-- Reuse the Modern Button Style from MainWindow -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="crimson"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DC140C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DC140C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#d3d3d3"/>
                                <Setter Property="Foreground" Value="#777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main layered Grid with background -->
    <Grid>
        <!-- Fallback background color -->
        <Rectangle Fill="#111111"/>

        <!-- Foreground layout -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Button row -->
                <RowDefinition Height="*"/>
                <!-- Content area -->
            </Grid.RowDefinitions>

            <!-- Top button row -->
            <UniformGrid Grid.Row="0" Columns="4" HorizontalAlignment="Stretch" Margin="0,10,0,0">
                <Button Content="YTDL" Style="{StaticResource ModernButtonStyle}" Click="BtnYTDL_Click"/>
                <Button Content="Installer" Style="{StaticResource ModernButtonStyle}" Click="BtnInstaller_Click"/>
                <Button Content="Scheduller" Style="{StaticResource ModernButtonStyle}" Click="BtnSchedule_Click"/>
                <Button Content="Option 4" Style="{StaticResource ModernButtonStyle}"/>
            </UniformGrid>

            <!-- Placeholder for sub-controls -->
            <ContentControl Grid.Row="1" x:Name="ContentArea" Margin="0"/>
        </Grid>
    </Grid>
</UserControl>
