<UserControl x:Class="phone_utils.AppManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450"
             Background="#661E1E1E"
             AllowDrop="True"
             Drop="UserControl_Drop"
             DragOver="UserControl_DragOver">

    <UserControl.Resources>
        <!-- Modern Button Style (reused from MainWindow/ScrcpyControl) -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#d3d3d3"/>
                                <Setter Property="Foreground" Value="#777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="Black"/>
            <!-- text color -->
            <Setter Property="Background" Value="#222"/>
            <!-- dark background -->
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="2"/>
        </Style>



        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#222"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#2a2a2a"
            CornerRadius="8"
            Padding="12"
            BorderBrush="Gray"
            BorderThickness="1">

        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Installed Apps -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="Installed Apps:" VerticalAlignment="Center"/>
                <ComboBox x:Name="CmbAndroidApps" Width="250" Margin="10,0,0,0" Style="{StaticResource DarkComboBox}"/>
                <Button Content="Refresh" Click="BtnRefresh_Click" Style="{StaticResource ModernButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
                <Button Content="Install APK" Click="BtnInstall_Click" Width="120" Style="{StaticResource ModernButtonStyle}"/>
                <Button Content="Uninstall" Click="BtnDelete_Click" Width="120" Style="{StaticResource ModernButtonStyle}" Margin="10,0,0,0"/>
                <Button Content="Open App" Click="BtnOpen_Click" Width="120" Style="{StaticResource ModernButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Status / Logs -->
            <TextBox x:Name="TxtStatus" Grid.Row="2" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Background="#222" Foreground="White" BorderBrush="#444"/>
        </Grid>

    </Border>
</UserControl>
